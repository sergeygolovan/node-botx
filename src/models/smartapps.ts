class SmartApp {
  constructor(
    public app_id: string,
    public enabled: boolean,
    public id: string,
    public name: string,
    public avatar?: string,
    public avatar_preview?: string
  ) {}
}
